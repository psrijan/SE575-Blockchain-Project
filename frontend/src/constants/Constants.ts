export const Endpoint = '/blockchain';

export const UrlConstants = {
    add: Endpoint + '/block',
    all: Endpoint + '/blocks',
    valid: Endpoint + '/valid',
    update: Endpoint + '/block/'
  };